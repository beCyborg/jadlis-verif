# TESTS.md — verif (v7, dual)

Живые зависимости: `codex` CLI (ChatGPT-подписка), `claude` CLI (headless), jq, python3 (третья ступень каскада Codex в normalize_and_merge.sh).

| Capability | State | Last run | Notes |
|---|---|---|---|
| Fable 5 headless + производная Claude-схема → structured_output | PASS | 2026-07-02 | смоук: mini + полная verdict-схема после jq-стрипа |
| Полная verdict.json БЕЗ стрипа на claude CLI | FAIL | 2026-07-02 | ожидаемо: minLength/minimum/maximum + корневые $schema/$id/title тихо отключают structured_output — поэтому prelude делает производную схему |
| Prelude echo моделей и effort одной строкой | PASS | 2026-09-21 | `MODELS=claude-fable-5-1 / gpt-6-astra / high / high` в блоке `VERIF_PATHS`: пин моделей виден в логе прогона, расхождение со скиллом ловится глазами |
| merge_verdicts.sh: N провайдеров / деградация до одного / битый JSON → stub / дубликаты меток | PASS | 2026-07-02 | юнит-тесты на синтетике |
| merge_verdicts.sh: `{}` от провайдера → stub unreliable (не crash) | PASS | 2026-08-13 | найден 2026-08-13: `{}` — валидный JSON без `.verdict` → rank(null) ронял весь merge («Cannot index object with null»). Фикс: pre-flight требует `.verdict`. Проверено: пустой вердикт ветки → consensus считается, стаб unreliable |
| render_merged.sh: динамический рендер по providers | PASS | 2026-09-21 | две ветки и деградация до одной |
| Арбитр: arbiter.json schema + arbiter.md на Fable 5 | PASS | 2026-07-02 | синтетические находки; оба F-id покрыты; выявил stdin-warning баг → `< /dev/null` |
| big-file guard (>350 KB → claude-opus-5) | PASS | 2026-07-02 | bash-логика на 400KB файле |
| Codex e2e (gpt-5.6-sol, --output-schema, xhigh, service_tier=default; локально priority — пин 15.08) | PASS | 2026-07-10 | ВАЖНО: exec пишет лог в stdout, verdict — ПОСЛЕДНЕЙ строкой → нормализация tail -1 (источник старых codex.clean.json). Смоук на codex-cli 0.144.1: валидный verdict за ~10s |
| Codex e2e (gpt-6-astra, --output-schema, xhigh, service_tier=default) | PASS | 2026-09-06 | Смоук на плане 4 КБ: EXIT=0, `verdict: needs-revision`, 5 находок, все required-поля `schema/verdict.json` на месте, в stdout нет usage-limit. **Регресс формы вывода:** Astra печатает финальный verdict МНОГОСТРОЧНЫМ JSON после лога прогона, а не одной последней строкой, как Sol → обе ступени каскада Codex в `normalize_and_merge.sh` (весь файл как JSON; `tail -1`) давали FAIL, и вердикт молча становился stub `unreliable`. Добавлена третья ступень: python сканирует файл с конца и берёт последний валидный объект с `verdict`. После правки merge даёт `codex → needs-revision, 5 findings`. |
| Codex e2e (gpt-6-astra, --output-schema, **high**, service_tier=default) | PASS | 2026-09-06 | Смоук после перевода `EFFORT_CODEX` с `xhigh` на `high` (ресёрч «Effort для плана и кода»): промпт `build_prompt.sh --type plan` на плане 1,3 КБ, `-c model_reasoning_effort="high"`, `CODEX_HOME=~/.claude/verif-homes/codex-home`. EXIT=0, вывод 166 КБ, `verdict: needs-revision`, 6 находок (3 high), все четыре required-поля `schema/verdict.json` (`verdict`, `summary`, `findings`, `next_steps`) на месте, usage-limit в stdout нет. Форма вывода прежняя (многострочный JSON после лога, строка ~1931) — третья ступень каскада в `normalize_and_merge.sh` по-прежнему обязательна. |
| e2e: верификаторы → merge → render на реальном плане | PASS | 2026-07-02 | precious-strolling-hopper (тогда три ветки): 6+10+9 находок, дедуп 25→21, consensus needs-revision |
| Полный dual e2e (Codex + Fable вживую) → merge → render → арбитр → интервью | PASS (авто-режим) | 2026-09-21 | гейт релиза 3.0.0: прогон на реальном плане, `providers=[codex,fable]`, развилка режима и оба режима интервью |
| Скриптовый регресс dual на сохранённом прогоне | PASS | 2026-09-21 | `2026-09-19--…--{codex,fable}.json` через `normalize_and_merge.sh` (копии в scratchpad): merged JSON **байт-в-байт** равен сохранённому (`jq -S` diff пуст), `verdict.md` отличается ровно одной строкой — ушла пометка «⚠ НЕ УЧАСТВОВАЛ» про удалённую третью ветку |
| Деградация до одной ветки: нет файла Fable → stub + рендер без ошибки | PASS | 2026-09-21 | `FABLE_OUT` указывает на несуществующий файл: rc=0, stderr пуст, `consensus: unreliable`, «⚠ СБОЙ: fable (ответ не JSON)»; merge только с меткой `codex` → «⚠ НЕ УЧАСТВОВАЛ: fable», consensus по выжившей ветке |
| `--only codex\|fable` single-рендер | PASS | 2026-07-02 | direct render без merge на e2e-артефакте (тогда проверялся и на третьей ветке) |
| Fable fallback-цепочка (claude-opus-5 с производной схемой) | CODED-NOT-VERIFIED | — | opus без стрипа падал (как и Fable); со стрипом не гонялся |
| Арбитр v6.1: поле `plain` (6 подполей) заполнено на синтетике, по-русски | PASS | 2026-08-21 | 2 синтетические находки (Redis-план) → Arb-2 с `cat arbiter.md plain-language.md` → 31 с, `.structured_output.assessments[].plain` — все 6 полей, русский, термин с пояснением («Redis (хранилище кэша)»); speculation → discuss/low |
| Интервью v6.1: вопросы по формату plain.* на реальном прогоне | PASS | 2026-08-21 | skill-zany-prism (прогон на двух ветках): «Коротко» перед отчётом; 13 вопросов, все по-русски; Codex+Fable findings по-русски; арбитр 168 с на 13 находок (vs 2–3 мин на 15–20 до v6.1); decisions.json как раньше. Пользователь: 6/6 apply принял, 4/4 skip принял, discuss 2→apply 1→skip |
| Интервью v7: блок `question` без заглушек | PENDING | — | на живом прогоне: все пять строк внутри окна AskUserQuestion, разметки нет нигде (`question`, `header`, опции), сегменты «Цена:» и «Риск:» отсутствуют там, где `plain.cost` / `plain.risk_of_fix` пустые, и ни одной заглушки «не оценивалась» / «время не оценивалось» / «не описан» / «ничем»; при `refuted:true` предупреждение первой строкой вопроса |
| Интервью v7: развилка режима авто/вручную | PASS (авто; ручной путь в новом формате — PENDING) | 2026-09-21 | на живом прогоне: развилка показана один раз (header «Режим», счётчики a/s/d сходятся с `arbiter.json`); «Авто» — ни одного вопроса по находкам, кроме ≤ 4 сильных развилок, перед применением напечатан компактный список решений, `decisions.json` несёт `decided_by` у каждого объекта и `auto_low_confidence:true` у остатка; «Вручную» — прежний протокол, по вопросу на находку; арбитр недоступен / покрыл не все id → развилки нет, одна строка объяснения, ручной режим |
| Арбитр v6.1: `refuted:true` на синтетике → `plain.problem` начинается с «Похоже, это не ошибка», вопрос получает первую строку-предупреждение | PASS | 2026-08-21 | синтетика Redis-план, 2 находки (одна ложная: «нет TTL» при TTL в плане) → 29 с; refuted=true с цитатой строки, plain.problem начинается правильно; `plain` НЕ в required, но заполнен у обоих id; risk_of_fix содержательный у обоих (после F6) |
| Plain-lint: запрещённые термины в `plain.*` арбитра (регрессия языка) | PASS | 2026-08-21 | `jq -r '.assessments[].plain \| .[]' AI/verif/<BASE>--arbiter.json \| grep -iE -c 'инвариант\|семантик\|персист\|детерминир\|калибров\|валидн\|артефакт\|гейт\|recall\|precision\|эмбеддинг\|payload\|идемпотент\|регресси\|дихотоми\|нормирован\|фоллбэк\|пайплайн'` → критерий 0; попадание с пояснением в скобках — допустимо, записать (прогон 21.08 skill-zany-prism: 1 попадание «пайплайна (цепочки шагов)» — с пояснением). Английские слова НЕ ловить — ложно срабатывает на именах файлов и команд. С 3.0.0 тем же прогоном ловить заглушки: `grep -c 'не оценивал\|не описан\|^ничем'` по `plain.cost` / `plain.risk_of_fix` → критерий 0 (пустое поле законно) |
| Калибровка арбитра: доля `skip`-рекомендаций, перевёрнутых пользователем в «Исправить» (контроль, что сдвиг в skip не глушит реальные находки) | PENDING | — | замер после 3–5 прогонов: `jq '[.[] \| select(.arbiter_recommendation=="skip")] \| map(.decision) \| group_by(.) \| map({k:.[0],n:length})' AI/verif/*--decisions.json`; при росте переворотов > 15 % — убрать цифру «0 %» из arbiter.md |
| render_merged.sh: ⚠ НЕ УЧАСТВОВАЛ / ⚠ СБОЙ | PASS | 2026-09-21 | merge только с меткой `codex` → «⚠ НЕ УЧАСТВОВАЛ: fable»; нечитаемый вывод ветки → «⚠ СБОЙ: fable (ответ не JSON)» |

## Gate

Перед объявлением релиза рабочим / изменением внешних шагов — PASS обязательны для строк:
«Полный dual e2e» (обе ветки вживую: `providers=[codex,fable]`, арбитр отработал, развилка режима и интервью прошли),
«Скриптовый регресс dual на сохранённом прогоне», «Деградация до одной ветки», «Интервью v7»,
плюс все уже-PASS строки повторно при смене версий CLI (`codex`, `claude --version`).
