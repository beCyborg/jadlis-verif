ADVERSARIAL DOCUMENT VERIFICATION

Read the file at {{TARGET_PATH}}. This is a guide/reference document generated by Claude Code (Opus 5).
Your job: find factual errors, outdated information, and misleading claims.

USER FOCUS: {{USER_FOCUS}}

VERIFICATION PROTOCOL:
1. VERSION ACCURACY — Check all version numbers, release dates, and feature availability claims against current official sources (web_search=live).
2. API/SYNTAX CHECK — Verify all code examples, CLI flags, and config options against current documentation. Flag deprecated or removed features.
3. URL VALIDATION — Check every URL for accessibility and accuracy of its description.
4. COMPLETENESS — What important features, caveats, or breaking changes are missing from this document?
5. MISLEADING CLAIMS — Are there statements that are technically true but practically misleading?

FINDING CATEGORIES (use in findings[].category):
- version_accuracy, api_syntax, url_validation, completeness, misleading_claims

OUTPUT CONTRACT:
Follow the policies in AGENTS.md — epistemic discipline (FACT/INFERENCE/SPECULATION labels in body), primary sources only, recency requirements. Emit ONLY JSON matching the --output-schema contract.

Verdict values:
- "approve" — doc is accurate and complete for its scope
- "needs-revision" — factual errors or significant gaps found, fix before publishing
- "unreliable" — enough errors that doc cannot be trusted, rewrite needed

For each finding set ALL fields: severity, title, body (with FACT/INFERENCE label and evidence chain), category, claim_type (fact|inference|speculation), confidence (0.0-1.0), recommendation, evidence_urls (array), source_date (ISO date for every version/date/deprecation claim, else null), file (path or null — typically the doc being checked), line_start/line_end (or null).

Do NOT rewrite the document. Only verify and critique.
